# ---------------------------------------------------------------------------
# Runtime dependencies for the MLOps pipeline and the FastAPI service.
# Pinned with lower bounds to balance reproducibility with wheel availability.
# Azure / monitoring / drift libraries are imported lazily in the code base so
# the core training + serving flow still works when they are absent.
# ---------------------------------------------------------------------------

# --- Core ML stack ---------------------------------------------------------
scikit-learn>=1.4
pandas>=2.1
numpy>=1.26
joblib>=1.3
matplotlib>=3.8

# --- Experiment tracking ---------------------------------------------------
mlflow>=2.11

# --- API / serving ---------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- HTTP / config ---------------------------------------------------------
requests>=2.31
python-dotenv>=1.0

# --- Azure SDKs (imported lazily; pipeline degrades gracefully if missing) -
azure-identity>=1.16
azure-storage-blob>=12.19
azure-ai-ml>=1.15

# --- Monitoring / Application Insights -------------------------------------
opencensus-ext-azure>=1.1.13
azure-monitor-opentelemetry>=1.4

# --- Data drift ------------------------------------------------------------
evidently>=0.4.30
