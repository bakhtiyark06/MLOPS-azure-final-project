# ---------------------------------------------------------------------------
# Development & CI dependencies. Installs everything in requirements.txt plus
# the testing, linting and coverage tooling used in CI.
# ---------------------------------------------------------------------------
-r requirements.txt

# --- Testing ---------------------------------------------------------------
pytest>=8.1
pytest-cov>=5.0
httpx>=0.27          # required by fastapi.testclient

# --- Quality tooling -------------------------------------------------------
ruff>=0.4
black>=24.3
